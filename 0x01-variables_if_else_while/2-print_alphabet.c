#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Prints letters of the alphabet
 * in lower case.
 *
 * Return: 0 (Success)
 */

int main(void)
{
	char letter = a;

	while (letter <= z)
	{
		putchar(letter);
		letter++;
	}
	putchar('\n');
	return (0);
}
