#include "main.h"

/**
 * _abs - function that computes the absolute value of an integer
 *
 * @n: int to compute as absolute
 *
 * Return: int.
 */

int _abs(int n)
{
	int n;

	if (n < 0)
	{
		return (-n);
	}
	else if (n > 0)
	{
		return (n);
	}
	else
	{
		return (n);
	}
}
